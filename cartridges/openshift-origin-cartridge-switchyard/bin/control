#!/bin/bash -e

source $OPENSHIFT_CARTRIDGE_SDK_BASH

function status() {
   client_result "SwitchYard is enabled. SwitchYard remains enabled until removed."
}

case "$1" in
  status)          status ;;
  *)               exit 0
esac
