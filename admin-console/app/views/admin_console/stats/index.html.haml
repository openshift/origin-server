- content_for :javascripts do
  :javascript
    $(function() {
      create_apps_per_domain_histogram();
      create_gears_per_user_histogram();
    });
.stat
  %h4
    Applications
  .stat-number
    = @stats[:application][:total]
  %h5
    Applications per Domain
  #apps_per_domain
.stat
  %h4
    Users
  .stat-number
    = @stats[:user][:total]
  %h5
    Gears per User
  #gears_per_user