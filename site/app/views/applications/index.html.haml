-# file: Applications index

%section.main
  %header
    %h1
      Applications
  .content
    - unless @userinfo.app_info.blank?
      = render 'applications/app_list_filter', name_filter: => @name_filter,
      = render 'applications/app_list', :apps => @filtered_apps
  %div.notifications
    <!-- tbd -->
